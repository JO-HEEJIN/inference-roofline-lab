[project]
name = "inference-roofline-lab"
version = "0.1.0"
description = "Reproducible experiments on LLM inference and accelerator kernels"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
